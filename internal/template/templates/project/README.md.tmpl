# {{.ProjectName}}

This microservice project was generated using the Microservice Bootstrapper.

## Architecture Overview

This project follows a microservices architecture with the following components:

{{if .Backend}}
### Backend Service
- **Technology**: {{.Backend.Technology}}
- **Port**: {{.Backend.Port}}
- **Location**: `./backend/`
{{end}}

{{if .Frontend}}
### Frontend Service  
- **Technology**: {{.Frontend.Technology}}
- **Port**: {{.Frontend.Port}}
- **Location**: `./frontend/`
{{end}}

{{if .Database}}
### Database Service
- **Technology**: {{.Database.Type}}
- **Port**: {{.Database.Port}}
- **Volume**: {{.Database.Volume}}
{{end}}

## Getting Started

### Prerequisites

Make sure you have the following installed:
- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

### Quick Start

1. **Clone and navigate to the project**:
   ```bash
   cd {{.ProjectName}}
   ```

2. **Set up environment variables**:
   ```bash
   cp .env.example .env
   ```
   Edit `.env` file with your specific configuration values.

3. **Start all services**:
   ```bash
   docker-compose up -d
   ```

4. **Verify services are running**:
   ```bash
   docker-compose ps
   ```

### Service URLs

Once all services are running, you can access them at:

{{if .Backend}}
- **Backend API**: http://localhost:{{.Backend.Port}}
{{if eq .Backend.Technology "fastapi"}}  - API Documentation: http://localhost:{{.Backend.Port}}/docs{{end}}
{{if eq .Backend.Technology "express"}}  - Health Check: http://localhost:{{.Backend.Port}}/health{{end}}
{{if eq .Backend.Technology "gin"}}  - Health Check: http://localhost:{{.Backend.Port}}/health{{end}}
{{end}}

{{if .Frontend}}
- **Frontend Application**: http://localhost:{{.Frontend.Port}}
{{end}}

{{if .Database}}
- **Database**: localhost:{{.Database.Port}}
{{end}}

## Development

### Starting Services

To start all services in development mode:
```bash
docker-compose up
```

To start services in the background:
```bash
docker-compose up -d
```

### Stopping Services

To stop all services:
```bash
docker-compose down
```

To stop and remove volumes (⚠️ **This will delete database data**):
```bash
docker-compose down -v
```

### Viewing Logs

To view logs from all services:
```bash
docker-compose logs -f
```

To view logs from a specific service:
```bash
{{if .Backend}}docker-compose logs -f backend{{end}}
{{if .Frontend}}docker-compose logs -f frontend{{end}}
{{if .Database}}docker-compose logs -f database{{end}}
```

### Rebuilding Services

If you make changes to Dockerfiles or dependencies:
```bash
docker-compose build
docker-compose up -d
```

## Project Structure

```
{{.ProjectName}}/
├── docker-compose.yml          # Docker orchestration configuration
├── .env.example               # Environment variables template
├── .env                       # Your local environment variables (git-ignored)
├── .gitignore                # Git ignore rules
├── README.md                 # This file
{{if .Backend}}├── backend/                   # Backend service
│   ├── Dockerfile            # Backend container configuration
{{if eq .Backend.Technology "fastapi"}}│   ├── requirements.txt      # Python dependencies
│   ├── main.py              # FastAPI application entry point
│   └── app/                 # Application source code{{end}}
{{if eq .Backend.Technology "express"}}│   ├── package.json         # Node.js dependencies
│   ├── server.js            # Express.js application entry point
│   └── src/                 # Application source code{{end}}
{{if eq .Backend.Technology "gin"}}│   ├── go.mod               # Go module definition
│   ├── main.go              # Gin application entry point
│   └── internal/            # Application source code{{end}}
{{end}}{{if .Frontend}}├── frontend/                  # Frontend service
│   ├── Dockerfile            # Frontend container configuration
{{if eq .Frontend.Technology "react"}}│   ├── package.json         # React dependencies
│   ├── public/              # Static assets
│   └── src/                 # React source code{{end}}
{{if eq .Frontend.Technology "vue"}}│   ├── package.json         # Vue.js dependencies
│   ├── public/              # Static assets
│   └── src/                 # Vue.js source code{{end}}
{{if eq .Frontend.Technology "angular"}}│   ├── package.json         # Angular dependencies
│   ├── angular.json         # Angular configuration
│   └── src/                 # Angular source code{{end}}
{{end}}└── docs/                      # Project documentation
    └── setup.md              # Detailed setup instructions
```

## Environment Configuration

The project uses environment variables for configuration. Key variables include:

{{if .Database}}
### Database Configuration
{{range $key, $value := .Database.Environment}}
- `{{$key}}`: {{$value}}
{{end}}
{{end}}

{{if .Backend}}
### Backend Configuration
- `BACKEND_PORT`: {{.Backend.Port}}
{{end}}

{{if .Frontend}}
### Frontend Configuration  
- `FRONTEND_PORT`: {{.Frontend.Port}}
{{end}}

See `.env.example` for a complete list of configurable variables.

## API Documentation

{{if .Backend}}
{{if eq .Backend.Technology "fastapi"}}
### FastAPI Backend
- **Interactive API Docs**: http://localhost:{{.Backend.Port}}/docs
- **OpenAPI Schema**: http://localhost:{{.Backend.Port}}/openapi.json
{{end}}

{{if eq .Backend.Technology "express"}}
### Express.js Backend
- **Health Check**: `GET /health`
- **API Base**: `http://localhost:{{.Backend.Port}}/api`
{{end}}

{{if eq .Backend.Technology "gin"}}
### Gin Backend
- **Health Check**: `GET /health`  
- **API Base**: `http://localhost:{{.Backend.Port}}/api`
{{end}}
{{end}}

## Troubleshooting

### Common Issues

1. **Port conflicts**: If you get port binding errors, check if the ports are already in use:
   ```bash
   {{if .Backend}}lsof -i :{{.Backend.Port}}{{end}}
   {{if .Frontend}}lsof -i :{{.Frontend.Port}}{{end}}
   {{if .Database}}lsof -i :{{.Database.Port}}{{end}}
   ```

2. **Docker issues**: Make sure Docker is running and you have sufficient resources allocated.

3. **Permission issues**: On Linux/macOS, you might need to run Docker commands with `sudo` or add your user to the docker group.

### Getting Help

- Check service logs: `docker-compose logs [service-name]`
- Restart services: `docker-compose restart [service-name]`
- Rebuild containers: `docker-compose build [service-name]`

## Contributing

1. Fork the repository
2. Create a feature branch: `git checkout -b feature-name`
3. Make your changes
4. Test your changes: `docker-compose up --build`
5. Commit your changes: `git commit -am 'Add feature'`
6. Push to the branch: `git push origin feature-name`
7. Submit a pull request

## License

This project is licensed under the MIT License - see the LICENSE file for details.